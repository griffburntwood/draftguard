# Comma-separated dashboard origins, without paths or trailing slashes.
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Set the hosted dashboard origin in the backend hosting dashboard.
# This example file is documentation; the app does not load it automatically.
