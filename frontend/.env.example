# Local development uses the Vite proxy.
VITE_API_BASE_URL=/api

# For deployment, set this to the backend's public HTTPS origin.
# Example: https://your-backend.example.com
# This address is public. Never put secret keys in VITE_ variables.
